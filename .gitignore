# Personal / local-only
TEST_CREDENTIALS.md
BACKEND_NOTES.md

# Editor / OS
.DS_Store
Thumbs.db
.vscode/
.idea/

# Frontend build / deps
frontend/.next/
frontend/node_modules/

# Backend build / deps
backend/node_modules/
backend/dist/

# Stray root lockfile (we use per-folder package.json)
/package-lock.json
